import "./index.css";
import Watch from "../Watch";
import Form from "../Form";

const Register = () => {
  return (
    <div className="register">
      <h3 className="register-title">⚡Sign up to receive flash promotions!</h3>
      <Watch />
      <Form />
    </div>
  );
};

export default Register;
